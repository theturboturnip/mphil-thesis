%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Thesis Meta-Information
%%

%% The title of the thesis:
\title{Investigating scalable vector models on capability-based hardware}

%% The full name of the author (e.g.: James Smith):
\author{Samuel Stark}

%% College affiliation:
\college{Clare College}

%% College shield (optional):
\collegeshield{CollegeShields/Clare}

%% Submission date (optional):
\submissiondate{June 2022}

%% You can redefine the submission notice:
% \submissionnotice{A badass thesis submitted on time for the Degree of PhD}

%% Declaration date:
\date{June 2022}

%% Word count
\begin{luacode*}
wordcount_cmd = "texcount -1 -sum -merge -q chapters.tex"

local pip=io.popen(wordcount_cmd)
wordcount=pip:read()
pip:close()
    
-- From http://www.computercraft.info/forums2/index.php?/topic/8065-lua-thousand-separator/
function format_thousand(v)
    local s = string.format("%d", math.floor(v))
    local pos = string.len(s) % 3
    if pos == 0 then pos = 3 end
    return string.sub(s, 1, pos)
    .. string.gsub(string.sub(s, pos+1), "(...)", ",%1")
end
\end{luacode*}
\wordcount{\luadirect{tex.sprint(format_thousand(wordcount))} words}
\wordcountmethod{\texttt{\luadirect{tex.sprint(wordcount_cmd)}}\\(Covers pages \pageref{wc:start}-\pageref{wc:end})}

%% PDF meta-info:
\subjectline{Computer Science}
\keywords{one two three}
